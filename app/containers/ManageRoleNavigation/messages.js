/*
 * ManageRoleNavigation Messages
 *
 * This contains all the text for the ManageRoleNavigation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ManageRoleNavigation";

export default defineMessages({
  saveMenuSuccessMessage: {
    id: `${scope}.saveMenuSuccessMessage`,
    defaultMessage: "Navigation Menu save successfully"
  },
  manageRoleNavigation: {
    id: `${scope}.manageRoleNavigation`,
    defaultMessage: "Manage Role Navigation"
  }
});
