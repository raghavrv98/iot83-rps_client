import React from 'react';
import { shallow } from 'enzyme';
// import { enzymeFind } from 'styled-components/test-utils';
// import { Dashboard } from '../index';
import history from 'utils/history';

describe('<Dashboard />', () => {
  // it('should test component when mounted', () => {
  //   spyOn(Dashboard.prototype, 'componentWillMount').and.callThrough();
  //   const wrapper = shallow(<Dashboard history={history} />);
  //   expect(wrapper).toBeDefined();
  //   expect(Dashboard.prototype.componentWillMount).toHaveBeenCalledTimes(1);
  // })

  // it('match snapshots', () => {
  //   const wrap = shallow(<Dashboard />)
  //   expect(wrap).toMatchSnapshot()
  // })
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
