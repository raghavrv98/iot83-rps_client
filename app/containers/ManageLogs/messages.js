/*
 * ManageLogs Messages
 *
 * This contains all the text for the ManageLogs container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ManageLogs";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ManageLogs container!"
  }
});
