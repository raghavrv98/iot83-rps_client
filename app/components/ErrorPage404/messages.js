import {defineMessages} from 'react-intl';

export const scope = "app.components.ErrorPage404";

export default defineMessages({
    error404:{
        id: `${scope}.error404`,
        defaultMessage: "404",
    }
})