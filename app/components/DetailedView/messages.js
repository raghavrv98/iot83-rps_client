/*
 * DetailedView Messages
 *
 * This contains all the text for the DetailedView component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.DetailedView";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DetailedView component!"
  }
});
