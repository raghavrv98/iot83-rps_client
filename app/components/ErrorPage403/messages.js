/*
 * ErrorPage403 Messages
 *
 * This contains all the text for the ErrorPage403 component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ErrorPage403";

export default defineMessages({
    error403: {
        id: `${scope}.error403`,
        defaultMessage: "403",
    }
});
