/*
 * ModalLoader Messages
 *
 * This contains all the text for the ModalLoader component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ModalLoader";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ModalLoader component!"
  }
});
