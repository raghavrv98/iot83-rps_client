/*
 * PipelineHealth Messages
 *
 * This contains all the text for the PipelineHealth component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.PipelineHealth";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PipelineHealth component!"
  }
});
